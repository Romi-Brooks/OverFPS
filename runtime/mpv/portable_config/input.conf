# OverFPS 快捷键
# 快捷键统一由 mpv 脚本管理 (portable_config\scripts\ 下的 lua):
#   Ctrl+i  开/关插帧补帧 (关闭后恢复原始帧率; 重开需等待 ~2s 模型加载)
#   Ctrl+s  开/关 Anime4K 超分 shader
#   Ctrl+d  开/关左上角 FPS/延迟 OSD
#   Ctrl+l  手动加载字幕 (扫描视频同目录 + subs\ 子目录)
# 其他常用 (mpv 默认):
#   v       切换字幕轨道   Shift+v 字幕显隐   . / ,  步进帧
